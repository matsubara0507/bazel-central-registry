bcr_test_module:
  module_path: "example"
  matrix:
    platform:
      - debian10
      - ubuntu2004
      - macos_arm64
      - windows
    bazel:
      - 7.x
      - 6.x
  tasks:
    verify_targets:
      name: "Run test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
        - "@rules_openapi//example:elm-api"
